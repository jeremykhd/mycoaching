Run the unit tests for this project and provide a clear summary.

1. Run `npm run test:unit` and capture output.
2. Report:
   - Total: pass / fail / skip counts
   - List any failing tests with the test name, file, and error message
   - If all pass: confirm and note any skipped tests
3. If there are failures:
   - Read the failing test file(s)
   - Read the source file(s) being tested
   - Diagnose the root cause
   - Propose a fix (ask before applying if the fix touches production code)

Do not modify test files to make tests pass artificially — fix the source or the test logic only if the test itself is wrong.
